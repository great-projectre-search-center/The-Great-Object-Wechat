<!--pages/AddressSelection/AddressSelection.wxml-->
<view class="text-cell">
  <!-- <van-cell 
    id="第一个地址"
    center title="{{nameAndphone1}}" 
    label="{{adder1}}" 
    bind:click="returnAdder"
  /> -->

  <view class="cel" bindtap="returnAdder" wx:for='{{addressList}}' id='{{item.province_Name}}{{item.city_Name}}{{item.area_Name}}{{item.address}}'>
    <view>{{item.name}} {{item.phone}}</view>
    <view>{{item.province_Name}}{{item.city_Name}}{{item.area_Name}}{{item.address}}</view>
  </view>

  
</view>

